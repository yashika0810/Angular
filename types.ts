let a:number;
a=1;
let b:boolean;
let c:string;
let e:number[] = [1,2,3]
let f:any[]=[1,true]

const ColorRed=0;
const ColorBlue=1;
const ColorGreen=2;
console.log(a);
enum Color {Red=6,Blue,Green};//enum is a container in which we can store related constants//
let backgroundColor = Color.Red;//background is a variable//enum member
